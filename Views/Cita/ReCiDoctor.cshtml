@model IEnumerable<TRABAJO_FINAL.Models.Horario>

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<html lang="es">
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" />
        <link rel="stylesheet" href="~/css/RegistroC.css" type="text/css" media="screen">
        <meta charset="UTF-8">
    </head>
    <body>

        <div class="box">
             <h1>Registro de citas </h1>
             
                 <div class="form-group">
                    <label >Nombre: </label>
                    <label >@TempData["nomb"] @TempData["apel"]</label>
                 </div>
                 <div class="form-group">
                    <label >Especialidad : </label>
                    <label >@TempData["esp"] [@TempData["cod"]]</label>
                 </div>
           
                 <div class="form-group">
                     <label>Doctor: </label>
                     <label >@TempData["nomdoc"] @TempData["apedoc"]</label>
                 </div>
                 @foreach (var item in Model){
                 <button>
                    <a asp-controller="Cita" asp-action="CitaTemp" />@item.FechaI a @item.FechaF</button> 
                 }
                
            

            

            @*foreach (var item in Model)

        asp-route-id="@item.IdHorarios"
                <button>
                    <a asp-controller="Cita" asp-action="CitaTemp" asp-route-id="@item.IdHorarios" />@item.FechaI a @item.FechaF</button> 
                
            }*@

        </div>

    </body>
</html>